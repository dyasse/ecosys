import {z} from 'zod'; export const newsletterSchema=z.object({email:z.string().email().max(255),locale:z.enum(['en','fr','ar']).default('en')}); export const checkoutSchema=z.object({items:z.array(z.object({productId:z.string().uuid(),variantId:z.string().uuid().optional(),quantity:z.number().int().min(1).max(20)})).min(1),email:z.string().email(),paymentMethod:z.enum(['stripe','cod']),idempotencyKey:z.string().min(16).max(128)}); export const contactSchema=z.object({name:z.string().min(2).max(100),email:z.string().email(),message:z.string().min(10).max(3000)});
