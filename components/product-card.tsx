'use client'; import Image from 'next/image'; import Link from 'next/link'; import { Heart, ShoppingBag, Star } from 'lucide-react'; import type { Product } from '@/lib/catalog';
export function ProductCard({product}:{product:Product}){return <article className="group overflow-hidden rounded-[18px] bg-white"><div className="relative aspect-square overflow-hidden bg-[#f6dde2]"><Image src={product.image} alt={product.name} fill sizes="(max-width: 900px) 50vw, 25vw" className="object-cover transition duration-500 group-hover:scale-105"/>{product.badge&&<span className="absolute left-3 top-3 rounded-full bg-white px-2 py-1 text-[10px] font-bold">{product.badge}</span>}<button aria-label={`Save ${product.name}`} className="absolute right-3 top-3 rounded-full bg-white p-2"><Heart size={16}/></button></div><div className="p-4"><p className="text-xs text-[#9b5162]">{product.brand}</p><Link href={`/product/${product.slug}`} className="mt-1 block min-h-12 font-semibold hover:text-[#9b5162]">{product.name}</Link><div className="mt-2 flex items-center gap-1 text-xs"><Star size={13} fill="#d4af37" strokeWidth={0}/><b>{product.rating}</b><span className="text-gray-500">({product.reviews})</span></div><div className="mt-3 flex items-center justify-between"><div><b>€{product.price.toFixed(2)}</b>{product.compareAt&&<del className="ml-2 text-xs text-gray-400">€{product.compareAt}</del>}</div><button onClick={()=>localStorage.setItem('azianline-cart',JSON.stringify([product]))} aria-label={`Add ${product.name} to cart`} className="rounded-full bg-[#f6dde2] p-2"><ShoppingBag size={16}/></button></div>{product.stock<10&&<p className="mt-2 text-xs text-[#9b5162]">Only {product.stock} left</p>}</div></article>}
