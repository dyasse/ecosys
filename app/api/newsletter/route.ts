import {NextResponse} from 'next/server'; import {newsletterSchema} from '@/lib/validation'; export async function POST(req:Request){const parsed=newsletterSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Please enter a valid email address.'},{status:400}); return NextResponse.json({ok:true,message:'You are on the Azianline list.'},{status:201})}
