'use client';import {useState} from 'react';export default function Contact(){const [status,setStatus]=useState<'idle'|'sending'|'sent'|'error'>('idle');return <main className="shell max-w-2xl py-16"><p className="eyebrow">We’re here</p><h1 className="serif text-5xl">Contact us</h1><p className="mt-3 text-gray-600">We typically respond within two working days.</p><form onSubmit={async e=>{e.preventDefault();setStatus('sending');const form=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});setStatus(r.ok?'sent':'error')}} className="card mt-8 grid gap-4 p-6"><input required name="name" placeholder="Your name" className="rounded-xl border p-3"/><input required name="email" type="email" placeholder="Email address" className="rounded-xl border p-3"/><textarea required name="message" minLength={10} placeholder="How can we help?" className="min-h-36 rounded-xl border p-3"/><button disabled={status==='sending'} className="btn btn-primary">{status==='sending'?'Sending…':'Send message'}</button>{status==='sent'&&<p role="status" className="text-sm text-[#9b5162]">Thank you — your message has been received.</p>}{status==='error'&&<p role="alert" className="text-sm text-red-700">Please review your details and try again.</p>}</form></main>}
