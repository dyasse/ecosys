import {notFound} from 'next/navigation'; import {categories,products} from '@/lib/catalog'; import {ProductCard} from '@/components/product-card';
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=categories.find(c=>c[0]===slug);if(!category)notFound();const list=products.filter(p=>p.category===slug);return <main className="shell py-10"><div className="rounded-[28px] bg-[#f6dde2] p-10"><p className="eyebrow">Azianline collection</p><h1 className="serif text-5xl">{category[1]}</h1><p className="mt-3 max-w-xl text-gray-600">{category[2]}</p></div><div className="mt-10 grid-products">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}
