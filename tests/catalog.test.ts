import {describe,it,expect} from 'vitest';import {getProduct,products} from '../lib/catalog';describe('catalogue',()=>{it('uses unique product slugs',()=>expect(new Set(products.map(x=>x.slug)).size).toBe(products.length));it('rejects unknown products',()=>expect(getProduct('unknown')).toBeUndefined());it('does not contain negative prices',()=>expect(products.every(x=>x.price>=0)).toBe(true))});
